package app.testero.repository;

import app.testero.entity.assessment.AssessmentStart;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AssessmentStartRepository extends JpaRepository<AssessmentStart, Long> {
}
